import { removeItem } from '../src/remove-item.js';

const test = QUnit.test;

test('removeItem() should', (expect) => {
    const expected = '';
    
    const actual = 'test not here yet';

    expect.equal(actual, expected);
});